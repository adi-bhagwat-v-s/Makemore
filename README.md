Makemore is a character level language model powered by a wide range of model from basic bigram to transformer (those used in GPT). A new set of words are generated based on the data passed in during the training of the algorithm.
This project was done with the mentorship of @Andrej_Karpathy. 
